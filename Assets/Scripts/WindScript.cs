using UnityEngine;
using System.Collections;

public class WindScript : MonoBehaviour {

	static public Vector2 windDirection;

	// Use this for initialization
	void Start () {
		windDirection = new Vector2(1,0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
